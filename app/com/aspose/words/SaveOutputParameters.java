package com.aspose.words;

/**
 * Shim for the value Aspose's {@code Document.save} returns (JCLAW-451). Cobalt's
 * {@code Medias} only needs the return type to exist — it discards the value — so
 * this is intentionally empty. See {@code package-info}.
 */
public class SaveOutputParameters {
}
